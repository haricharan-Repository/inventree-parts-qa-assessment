# Copy to .env and adjust for your InvenTree instance.
# Default values match a fresh `docker compose up` InvenTree install.

BASE_URL=http://localhost:8000
INVENTREE_USERNAME=admin
INVENTREE_PASSWORD=inventree
